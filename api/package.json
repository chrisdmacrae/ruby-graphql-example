{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "./bin/setup",
    "dev": "RAILS_ENV=development ./bin/bundle exec rails server -p 3010",
    "start": "RAILS_ENV=production ./bin/bundle exec rails server -p 3010"
  }
}
